// generated by tools/shaders.php — do not edit
#include <metal_stdlib>
#include <simd/simd.h>

using namespace metal;

struct CubeParams
{
    float4 dir;
};

struct fs_out
{
    float4 FragColor [[color(0)]];
};

fragment fs_out fs(constant CubeParams& _17 [[buffer(2)]], texturecube<float> uCube [[texture(0)]], sampler uCubeSmplr [[sampler(0)]])
{
    fs_out out = {};
    out.FragColor = float4(uCube.sample(uCubeSmplr, fast::normalize(_17.dir.xyz), level(_17.dir.w)).xyz, 1.0);
    return out;
}

