// generated by tools/shaders.php — do not edit
#include <metal_stdlib>
#include <simd/simd.h>

using namespace metal;

struct LodParams
{
    float4 level0;
};

struct fs_out
{
    float4 FragColor [[color(0)]];
};

struct fs_in
{
    float2 vUv [[user(locn0)]];
};

fragment fs_out fs(fs_in in [[stage_in]], constant LodParams& _21 [[buffer(2)]], texture2d<float> uTexture [[texture(0)]], sampler uTextureSmplr [[sampler(0)]])
{
    fs_out out = {};
    out.FragColor = float4(uTexture.sample(uTextureSmplr, in.vUv, level(_21.level0.x)).xyz, 1.0);
    return out;
}

