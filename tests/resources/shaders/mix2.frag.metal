// generated by tools/shaders.php — do not edit
#include <metal_stdlib>
#include <simd/simd.h>

using namespace metal;

struct fs_out
{
    float4 FragColor [[color(0)]];
};

struct fs_in
{
    float2 vUv [[user(locn0)]];
};

fragment fs_out fs(fs_in in [[stage_in]], texture2d<float> texA [[texture(0)]], texture2d<float> texB [[texture(1)]], sampler texASmplr [[sampler(0)]], sampler texBSmplr [[sampler(1)]])
{
    fs_out out = {};
    float3 a = texA.sample(texASmplr, in.vUv).xyz;
    float3 b = texB.sample(texBSmplr, in.vUv).xyz;
    out.FragColor = float4((a * 0.5) + (b * 0.5), 1.0);
    return out;
}

