/**
 * Color attachment arithmetic. GPU-free.
 */

use visu::graphics::ColorAttachment;

test color_attachment_adds
{
    ColorAttachment $a = ColorAttachment(0) + 3;
    assert($a->index == 3, '0 + 3');
}
