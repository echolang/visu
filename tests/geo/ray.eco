/**
 * Ray point-at-distance.
 */

use visu::geo::Ray;
use visu::math::{Vec3, equal};

test ray_at_distance
{
    Ray $r = Ray(Vec3(1.0f, 2.0f, 3.0f), Vec3(0.0f, 1.0f, 0.0f));
    assert(equal($r->at(4.0f), Vec3(1.0f, 6.0f, 3.0f)), 'at');
}
