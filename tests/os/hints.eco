/**
 * WindowHints defaults are GLFW_NO_API.
 */

use visu::os::WindowHints;

test hints_defaults_are_no_api
{
    WindowHints $h = WindowHints::defaults();
    int32 $api = $h->clientAPI ?? -1;
    assert($api == 0, 'no api');
}
