/**
 * `str::from` so `"{$e}"` works. Own file because a namespace is a
 * property of the file.
 */

namespace str;

public function from(const visu::vg::VgError& $e) : string
{
    return $e->message();
}
