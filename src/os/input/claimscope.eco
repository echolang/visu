/**
 * What an input claim takes. A pointer claim leaves the keyboard to action maps and key
 * polling; an `all` claim (the console, a focused text field) takes both.
 */

namespace visu::os::input;

public enum ClaimScope
{
    case pointer;
    case all;
}
