/**
 * `str::from` so `"{$e}"` works. Own file because a namespace is a
 * property of the file.
 */

namespace str;

public function from(const visu::quickstart::AppError& $e) : string
{
    return $e->message();
}
