/**
 * Queue a scene switch. The Director listens and adopts `$next`
 * after present; last write wins if several fire in one frame.
 */

namespace visu::scene;

public struct Switch
{
    public const Scene $next;
}
