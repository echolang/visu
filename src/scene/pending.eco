/**
 * A queued scene. Constructed scenes own nothing; built ones already hold GPU
 * (a loader target) and are unloaded if a later switch discards them.
 */

namespace visu::scene;

public enum ScenePending
{
    case none;
    case constructed(Scene $scene);
    case built(Scene $scene);
}
