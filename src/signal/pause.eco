/**
 * The app is leaving the foreground.
 * The host pauses the clock; GameLoop snaps on ResumeSignal so the next pulse is not a catch-up burst.
 */

namespace visu::signal;

public struct PauseSignal
{
}
