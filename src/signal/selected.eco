/**
 * An entity was picked (dev picker today).
 */

namespace visu::signal;

use visu::ecs::Entity;

public enum Pick
{
    case picker;
}

public struct EntitySelectedSignal
{
    public const Entity $entity;
    public const Pick $pick;
}
