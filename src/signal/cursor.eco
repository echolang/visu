/**
 * Cursor moved.
 * Offsets are since the previous cursor event.
 */

namespace visu::signal;

public struct CursorSignal
{
    public const float64 $x;
    public const float64 $y;
    public const float64 $offsetX;
    public const float64 $offsetY;
}
