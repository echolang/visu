/**
 * What a handler returns. Payloads are facts; this is how a listener
 * stops the rest of the chain (PHP mutated Signal::stopPropagation).
 */

namespace visu::signal;

public enum Flow
{
    case proceed;
    case stop;
}
