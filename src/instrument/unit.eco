/**
 * Unit for a named gauge.
 * Built-in loop / memory samples do not go through this; they are typed fields on Sample.
 */

namespace visu::instrument;

public enum Unit
{
    case fps;
    case ns;
    case bytes;
    case count;
    case ratio;
}
