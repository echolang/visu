/**
 * The game loop drives this.
 * update() may run 0..n times per frame (fixed step); render() runs once with an interpolation factor.
 */

namespace visu::runtime;

public interface GameLoopDelegate
{
    function update() : void;
    function render(float32 $compensation) : void;
    function shouldStop() : bool;
}
