/**
 * `str::from` so `"{$e}"` works.
 * Own file because a namespace is a property of the file.
 */

namespace str;

public function from(const visu::ecs::Entity& $e) : string
{
    return "{$e->index()}v{$e->version()}";
}
