/**
 * `hash::of` so Entity can be a map key.
 * Own file because a namespace is a property of the file.
 */

namespace hash;

public function of(const visu::ecs::Entity& $e) : uint64
{
    return hash::of($e->id);
}
