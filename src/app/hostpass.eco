/**
 * Per-frame mailbox for the offscreen scene: the target the app draws into, and the color the host blits to the backbuffer.
 * Replace outputTexture to inspect a G-Buffer.
 */

namespace visu::app;

use visu::graphics::{TargetRef, TextureRef};

public class HostPassData
{
    public TargetRef $renderTarget;
    public TextureRef $outputTexture;

    public constructor(TargetRef $renderTarget, TextureRef $outputTexture)
    {
        $this->renderTarget = $renderTarget;
        $this->outputTexture = $outputTexture;
    }
}
