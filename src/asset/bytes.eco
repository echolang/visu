/**
 * Identity decoder. Kind `bytes`; the catalog payload is the live object.
 */

namespace visu::asset;

public const string KIND_BYTES = 'bytes';

public function decodeBytes(Assets $assets, Blob $blob) : result<Blob, AssetError>
{
    return .ok($blob);
}
