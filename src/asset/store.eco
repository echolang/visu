/**
 * Where cooked bytes live. Implementations are classes; only a class can be stored as an interface value.
 */

namespace visu::asset;

public interface Store
{
    function find(string $id) : CatalogEntry?;
    function read(const CatalogEntry& $entry) : result<Blob, AssetError>;
    function entries() : array<CatalogEntry>;
}
