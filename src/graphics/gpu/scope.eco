/**
 * Token for a debug-label group opened by `gpuBegin`. `$index` is -1 when the
 * label was not opened (compiled out, unsupported, or no slot).
 */

namespace visu::graphics;

public struct GpuProfileScope
{
    public int32 $index = -1;
}
