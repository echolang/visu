/**
 * How much GPU instrumentation is on. `detailed` turns on Metal encoder sample
 * barriers (`withBarrier:YES`); Apple silicon still reports stage-boundary
 * timestamps. Vulkan ignores detailed.
 */

namespace visu::graphics;

public enum GpuProfilerMode
{
    case off;
    case lightweight;
    case detailed;
}
