/**
 * Which queue a GPU profile event ran on. visu records graphics, a Metal
 * pre-render compute command buffer, and copies; async compute is a later queue.
 */

namespace visu::graphics;

public enum GpuQueue
{
    case graphics;
    case compute;
    case copy;
}
