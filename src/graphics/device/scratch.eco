/**
 * Host-visible slice of this frame's uniform/staging ring.
 */

namespace visu::graphics;

public struct ScratchSlice
{
    public ptr<uint8> $data;
    public uint64 $offset;
    public uint64 $bytes;
    public uint32 $frameSlot;
}
