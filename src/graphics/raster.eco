/**
 * Per-pipeline raster state. Applied through Gpu so redundant
 * enable/disable calls are skipped.
 */

namespace visu::graphics;

public enum CullMode
{
    case none;
    case front;
    case back;
}

public enum BlendMode
{
    case none;
    case alpha;
}

public struct Raster
{
    public bool $depthTest = false;
    public bool $depthWrite = true;
    public CullMode $cull = .none;
    public BlendMode $blend = .none;
    public bool $colorWrite = true;

    public static function opaque() : Raster
    {
        return Raster(true, true, .back, .none, true);
    }

    public static function alpha() : Raster
    {
        return Raster(true, false, .none, .alpha, true);
    }

    public static function none() : Raster
    {
        return Raster(false, false, .none, .none, true);
    }
}
