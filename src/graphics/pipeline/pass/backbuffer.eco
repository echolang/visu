/**
 * The window backbuffer, published by Pipeline's constructor.
 */

namespace visu::graphics;

public class Backbuffer
{
    public TargetRef $target;

    public constructor(TargetRef $target)
    {
        $this->target = $target;
    }
}
