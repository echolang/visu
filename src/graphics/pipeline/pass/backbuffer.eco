/**
 * The window backbuffer, published by Pipeline's constructor. A fact:
 * one producer, N readers.
 */

namespace visu::graphics;

public class Backbuffer
{
    public const TargetRef $target;

    public constructor(TargetRef $target)
    {
        $this->target = $target;
    }
}
