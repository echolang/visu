// generated by tools/shaders.php — do not edit
#include <metal_stdlib>
#include <simd/simd.h>

using namespace metal;

struct CubeDraw
{
    float4x4 u_model;
    float4 u_color;
};

struct CameraUniforms
{
    float4x4 u_projection;
    float4x4 u_view;
    float4x4 u_projection_view;
    float4x4 u_inverse_projection;
    float4x4 u_inverse_projection_view;
    float4 u_camera_position;
    float4 u_resolution;
};

struct vs_out
{
    float3 vPos [[user(locn0)]];
    float4 vColor [[user(locn1)]];
    float4 gl_Position [[position]];
};

struct vs_in
{
    float3 aPos [[attribute(0)]];
};

vertex vs_out vs(vs_in in [[stage_in]], constant CubeDraw& _13 [[buffer(2)]], constant CameraUniforms& _47 [[buffer(3)]])
{
    vs_out out = {};
    float4 world = _13.u_model * float4(in.aPos, 1.0);
    out.vPos = world.xyz;
    out.vColor = _13.u_color;
    out.gl_Position = _47.u_projection_view * world;
    return out;
}

